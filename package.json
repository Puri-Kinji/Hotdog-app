{
  "name": "earles-hotdog-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "sync": "npx cap sync",
    "android": "npx cap sync android && npx cap open android",
    "ios": "npx cap sync ios && npx cap open ios"
  },

  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "@capacitor/core": "^7.4.4"
  },

  "devDependencies": {
    "@capacitor/cli": "^7.4.4",
    "@capacitor/android": "^7.4.4",
    "@capacitor/ios": "^7.4.4",

    "@vitejs/plugin-react": "^5.0.4",
    "typescript": "^5.6.3",
    "vite": "^5.3.0"
  }
}
